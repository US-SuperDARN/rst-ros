export RADAR_NAME='cve'     # three-letter radar code
export RADAR_CH1='a'        # single-letter channel 1 code (a-d)
export RADAR_CH2='b'        # single-letter channel 2 code (a-d)

export MAIN_RADAR='true'    # true if main radar computer, false if remote computer
export TWO_CHANNEL='true'   # true if two channels active, false for single channel
export TWO_RADAR='true'     # true if dual radar site, false if single radar

export RT_PORT1='1024'      # local real-time port for channel 1
export RT_PORT2='1025'      # local real-time port for channel 2
